my-consent-form-project/
│
├── api/
│   └── sendConsentEmail.js      ← Your backend serverless function here
│
├── public/
│   ├── uncdf_logo.png
│   ├── foundation_logo.png
│   └── (any other static assets)
│
├── index.html                  ← Your frontend form page
│
├── package.json                ← npm dependencies (e.g. nodemailer)
│
└── vercel.json                 ← (optional) Vercel config file
